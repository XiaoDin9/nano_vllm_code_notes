prepare_prefill 函数的解释：
Sequence
token0 ................................ token699

     cached                compute
---------------|---------------------------
0             256                         700

Query: [256~699]
Key:   [0~699]

KV Cache:
    Logical blocks: block0       block1       block2
    Physical blocks:  7            15           3
                    cached        write        write
    slot_mapping:               3840~4095     768~955

FlashAttention:
    Q = new tokens
    K = all tokens


store_kvcache 函数的解释：
Attention:
    new token attends all history
        Transformer Forward

      key/value tensor (连续内存)
        shape=[N, num_heads, D]

 ┌───────────────────────────────────────┐

 │ token0 │ token1 │ token2 │ ...       │

 └───────────────────────────────────────┘
                     │
                     │ key.stride(0)
                     ▼
          Triton Kernel Launch
        store_kvcache_kernel[(N,)]

worker0       worker1       worker2
 token0        token1        token2
     │             │             │
     │             │             │
     ▼             ▼             ▼

slot_mapping
   [1792]        [900]        [3050]

     │             │             │
     ▼             ▼             ▼
          KV Cache Memory Pool
 ┌─────────────────────────────────────────────┐

 │slot900  │ slot1792 │ slot3050 │ ...        │
 │ token1  │ token0   │ token2   │            │

 └─────────────────────────────────────────────┘
         Scatter Write (非连续写入)